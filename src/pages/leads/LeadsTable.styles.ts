import styled from "@emotion/styled";

const LeadsTableStyled = styled.div`
  .lead-item {
    border-radius: 4px;
    /* border: ${(props) => {
      // @ts-ignore
      return `2px solid ${props.theme.colors.blue[5]}`;
    }}; */
    /* box-shadow */

    padding: 1rem;
    margin-bottom: 1rem;
    font-size: 1rem;
  }
`;

export default LeadsTableStyled;
